
### ✅ **Что такое замыкание**

> **Определение:** Замыкание — это комбинация функции и лексического окружения, в котором эта функция была создана. Это окружение состоит из всех локальных переменных, которые были в области видимости функции во время её создания.

**Зачем это нужно:**

- Инкапсуляция данных — можно ограничить доступ к данным, храня их в приватной области.
    
- Создание функций с «памятью» — функции запоминают своё окружение даже после выхода из области видимости.
    

---

### 🔑 **Простой пример**

```javascript
function outer() {
  let count = 0; // локальная переменная
  return function inner() {
    count++;
    console.log(count);
  };
}

const counter = outer();

counter(); // 1
counter(); // 2
counter(); // 3
```

▶️ Здесь `inner` — замыкание, которое «помнит» переменную `count` из `outer`.

---

### ⚛️ **Пример на React**

В React замыкания часто встречаются внутри обработчиков событий и хуков:

```jsx
import React, { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);

  const increment = () => {
    // increment замыкает переменную count из useState
    setCount(count + 1);
  };

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={increment}>Add</button>
    </div>
  );
}

export default Counter;
```

▶️ `increment` — это замыкание, которое видит `count` из компонента.

---

### ⚙️ **Как это работает в браузере**

- Функция создаётся вместе с замороженной ссылкой на своё окружение.
    
- Даже если внешняя функция завершилась, память не освобождается, пока замыкание существует.
    

---

### 📖 **Полезные статьи**

- [MDN — Closures](https://developer.mozilla.org/ru/docs/Web/JavaScript/Closures) (основная теория на русском)
- [freeCodeCamp — Understanding Closures](https://www.freecodecamp.org/news/closures-in-javascript-with-examples/) (на английском, с примерами)
- [React Docs — Rules of Hooks](https://react.dev/learn/rules-of-hooks) — как замыкания используются в хуках.
- [Видео по теме](https://youtu.be/1m3AXMWk_g8?si=kxkpXlhNDyeDIjWz) - Миша Ларченко на простых примерах объясняет замыкания.



---

### 🗂️ **Иллюстрация**


![[Pasted image 20250619085717.png]]

---

## ✅ **Вывод**

- Замыкания = функция + её окружение
    
- Применяются для приватности и создания функций с памятью
    
- Повсеместно используются в React в `useState`, `useEffect` и обработчиках событий
    

---

### Замыкания (Closures)

**Что такое замыкания?** Замыкание – это комбинация функции и того окружения (лексического окружения), в котором эта функция была создана. Замыкания позволяют инкапсулировать или ограничить доступ к данным, которые используются теми или иными функциями. Это означает, что внутренняя функция "помнит" переменные из области видимости внешней функции, даже после того, как внешняя функция завершила свое выполнение.

**Зачем нужны замыкания?** Замыкания очень важны в программировании, особенно в JavaScript, поскольку они позволяют повторно использовать одни и те же функции или код для обработки различной информации. Информация, "захваченная" замыканием, остается скрытой внутри функции, и каждый раз при вызове этой функции (после ее инициализации) она будет использовать ту же самую сохраненную информацию.

**Пример замыкания:**

Давайте рассмотрим пример на JavaScript, который шаг за шагом демонстрирует концепцию замыкания, как это описано в источниках.

Представим, что у нас есть HTML-страница с двумя кнопками:

```
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Пример Замыкания</title>
</head>
<body>
    <button id="misha">Миша</button> <!-- -->
    <button id="sasha">Саша</button> <!-- -->

    <script>
        // Код JavaScript будет здесь
    </script>
</body>
</html>
```

А вот соответствующий JavaScript-код:

```
// Функция `init` создает окружение для внутренней функции `showName`
function init(username) { //
    // `showName` - это внутренняя функция
    function showName() { //
        // `showName` использует переменную `username` из внешнего окружения (`init`)
        console.log("Привет, " + username); //
    }
    // `init` возвращает `showName`
    return showName; //
}

// Получаем ссылки на кнопки по их ID
const mishaButton = document.getElementById("misha"); //
const sashaButton = document.getElementById("sasha"); //

// Присваиваем обработчики кликов:
// `init("Миша")` вызывается и возвращает функцию `showName`,
// которая "помнит" `username` как "Миша". Эта возвращенная функция
// затем назначается обработчиком события `onclick` для кнопки "Миша".
mishaButton.onclick = init("Миша"); //

// Аналогично для кнопки "Саша"
sashaButton.onclick = init("Саша"); //

// Когда пользователь нажимает кнопку "Миша",
// вызывается функция, которую вернула `init("Миша")`,
// и она выводит "Привет, Миша".
// Когда пользователь нажимает кнопку "Саша",
// вызывается функция, которую вернула `init("Саша")`,
// и она выводит "Привет, Саша".
```

В этом примере функция `init` при вызове с аргументом `username` создает внутреннюю функцию `showName`. Важно то, что `showName` захватывает значение `username` из своего родительского окружения (`init`). Даже после того, как `init` закончила выполнение и вернула `showName`, `showName` все еще имеет доступ к переменной `username`, которая была передана в `init` при её первоначальном вызове. Это и есть замыкание.

### Чистые функции (Pure Functions)

**Что такое чистые функции?** Чистая функция – это функция, которая использует только те данные, которые поступают к ней в качестве аргументов, и никак не зависит от того, что происходит вокруг (то есть от внешних переменных или состояния). При каждом вызове с одними и теми же аргументами чистая функция всегда будет возвращать один и тот же результат.

**Пример чистой функции:**

Рассмотрим пример, который в источнике "[Russian (auto-generated)] Пойми замыкания в JavaScript за 5 минут и удиви друзей! [DownSub.com].txt" используется как отправная точка для объяснения замыканий:

```
function show(username) { //
    console.log("Привет, " + username); //
}

// Примеры вызова:
show("Миша"); // Выведет: "Привет, Миша"
show("Саша"); // Выведет: "Привет, Саша"
```

Эта функция `show` является чистой, потому что ее результат зависит исключительно от переданного аргумента `username`. Она не читает и не изменяет какие-либо внешние переменные или состояния.

**Сравнение с "нечистой" функцией (почти замыкание):**

Источник также показывает пример функции, которая не является чистой и становится "почти замыканием":

```
let username = "Саша"; // Внешняя переменная

function show() { // Функция не принимает аргументов
    console.log("Привет, " + username); // Использует внешнюю переменную `username`
}

// Примеры вызова:
show(); // Выведет: "Привет, Саша"
username = "Ольга";
show(); // Выведет: "Привет, Ольга" (результат изменился из-за изменения внешней переменной)
```

В этом случае функция `show` не является чистой, так как она полагается на внешнюю переменную `username`. Ее результат может меняться, если значение этой внешней переменной изменится. Это называется "почти замыканием", потому что функция использует внешнюю переменную, но так как переменная и функция находятся в глобальной области видимости, любой другой скрипт на странице может видеть и изменять эту переменную, что не обеспечивает инкапсуляции, характерной для полных замыканий.

### React и другие фреймворки

Источники не содержат прямых примеров кода на React, иллюстрирующих замыкания или чистые функции. Однако, в источнике "Patterns.dev" упоминаются "Hooks Pattern" в React и "Composables" в Vue.js, которые часто используют замыкания для сохранения состояния и логики между рендерами компонентов. Например, когда вы используете хук `useState` или `useCallback` в React, функции, которые вы получаете в ответ, "замыкаются" над значениями переменных из того рендера, в котором они были созданы.

### Изображения и ссылки на статьи

Предоставленные источники являются текстовыми выдержками и транскрипциями. Они не содержат прямых изображений или гиперссылок на внешние статьи. Однако, источник "[Russian (auto-generated)] Пойми замыкания в JavaScript за 5 минут и удиви друзей! [DownSub.com].txt" упоминает, что "вы сейчас видите перед собой код слева и страницу справа", что подразумевает наличие визуальных материалов в оригинальном видео, из которого была взята транскрипция.